[package]
name = "mpz-justvengers-core"
version = "0.1.0-alpha.1"
edition = "2024"
description = "Core primitives for Justvengers VOLE-based ZK protocol"

[lints]
workspace = true

[features]
default = []
rayon = ["dep:rayon"]
cl-scheme = []

[dependencies]
mpz-core = { workspace = true }
mpz-fields = { workspace = true }

thiserror = { workspace = true }
serde = { workspace = true, features = ["derive"] }
rayon = { workspace = true, optional = true }
rand = { workspace = true }

# Class group cryptography for CL-based homomorphic encryption
class_group = { path = "/home/default2/Desktop/gh_repos/class-group" }
num-bigint = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
criterion = { workspace = true }
rstest = { workspace = true }

[[bench]]
name = "rotation_bench"
harness = false
